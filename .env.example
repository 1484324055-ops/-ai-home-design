# 数据库连接字符串（PostgreSQL）
# 本地开发使用 SQLite: file:./dev.db
# 生产环境使用 PostgreSQL: postgresql://user:password@host:port/database
DATABASE_URL="file:./dev.db"

# JWT 密钥（生产环境务必更换为随机字符串）
JWT_SECRET="your-secret-key-change-in-production"
